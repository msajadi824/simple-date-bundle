{% block jSDate_widget -%}
    {% set defaults = {
        'data-is-rtl': 'true',
        'data-change-month': 'true',
        'data-change-year': 'true',
        'data-date-format': clientFormat,
        'class': attr.class|default('') ~ ' jSDate',
    } %}

    {% set attr = defaults|merge(attr) -%}
    {{- block('form_widget_simple') -}}
{%- endblock jSDate_widget %}